Host: trino

Критичний момент: Не localhost і не 127.0.0.1. Redash живе у своєму контейнері, тому він має звертатися до бази за внутрішнім DNS-іменем сервісу, яке ти прописав у docker-compose-ext.yaml.

Port: 8080

Username: redash (або admin. У нашому базовому конфігу Trino зараз немає жорсткої автентифікації, але це поле не може бути порожнім).

Catalog: minio (це назва конектора до S3, яку ти створив).

Schema: audit (опціонально, але краще вказати одразу, щоб не писати префікси в кожному запиті).

Password: залиш порожнім (якщо ти не налаштовував HTTPS/LDAP для Trino).